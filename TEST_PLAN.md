# 车辆电路图检索 Agent — 测试方案

> 适用范围：本地方案（FastAPI + Milvus + LangGraph）的**端到端接口验收**。
> 不替代 `pytest`（36 条单元/契约测试）与 `eval/`（离线检索指标），三者互补：
> `pytest` 验证代码契约，本方案验证**真实链路**，`eval/` 验证**检索质量**。

## 1. 前置条件

| 项 | 要求 |
|---|---|
| FastAPI | 在 `--port 8010` 运行，且**启动时的 CWD = 项目根目录**（`.env` 相对 CWD 解析） |
| `.env` | `DEEPSEEK_API_KEY` 已填且有效；**修改后必须重启进程**（`get_settings()` 有 `@lru_cache`） |
| Docker | etcd / MinIO / Milvus 均 healthy |
| Milvus | `circuit_documents_dense`、`circuit_documents_hybrid` 各 4233 条 |
| 依赖 | `.venv` 已安装 `pip install -e ".[dev]"` |

> ⚠️ 本方案会**真实调用 DeepSeek**（约 10 次）并用 CPU 跑 Reranker，单次检索 15～35 秒，
> 全套约 3～6 分钟。首次运行含模型冷启动，会更慢。

## 2. 分层用例

### L0 服务与静态资源（无需 LLM，秒级）

| ID | 输入 | 期望 |
|---|---|---|
| T01 | `GET /api/health` | 200 且 `{"status":"ok","version":"0.3.0"}` |
| T02 | `GET /` | 200，返回 HTML 首页 |
| T03 | `GET /css/style.css`、`GET /js/chat.js` | 200（前端静态挂载正常） |

### L1 请求校验

| ID | 输入 | 期望 |
|---|---|---|
| T04 | `POST /api/chat` `message=""` | 422（Pydantic `min_length=1`） |
| T05 | `POST /api/chat` `message="   "` | 200 且 `code=0`、`msg=消息内容不能为空` |

### L2 分流与检索直答

| ID | 输入 | 期望 |
|---|---|---|
| T06 | `POST /api/chat` `"你好"` | `code=1`、`type=text`、不触发检索（应 < 10s） |
| T07 | `POST /api/chat` `"东风天龙仪表针脚定义"` | `code=1`、`type=result`、`1 ≤ documents ≤ 5`；每条含 `id`/`fileName`/`hierarchyPath` |

### L3 澄清多轮（核心）

| ID | 输入 | 期望 |
|---|---|---|
| T08 | `POST /api/chat` `"东风天龙仪表"` | `code=1`、`type=options`、选项数 2～5，每项含 `id`/`text`/`value` |
| T09 | **选项独立性**：3 个新会话各自只点第 1/2/3 个选项 | 三组结果**互不相同**，且每组条数与该选项标注的条数一致 |
| T10 | **改选**：同一会话先点第 1 项，再点第 2 项 | 改选后返回**该选项自己的**结果集（既不静默重复，也不报错） |
| T11 | 非法 `optionValue`（会话处于澄清中） | `code=0`、`msg` 含"无效的选择项" |
| T12 | 已结束 / 无可用 interrupt 的 `sessionId` 直接 `/api/select` | `code=0`、`msg` 含"没有可用的选择项" |
| T14 | 澄清选项标签 | 标签来自真实资料类型或标题，**不含"第 N 组"** |
| T15 | 多轮澄清 `"4HK1发动机电脑板针脚定义"` + 点"返回上一步" | 第二轮提供返回项，点击后回到第一轮选项 |
| T16 | **跨轮次改选**：第二轮出结果后，回头点第一轮另一个选项 | 返回**该选项自己的**结果集（条数与该选项标注一致） |

### L4 已知缺陷回归

| ID | 输入 | 期望（当前实际行为） |
|---|---|---|
| T13 | `POST /api/chat` `"徐工仪表线路图"` | 当前落到"没有找到匹配资料"，但无过滤检索有命中 → **假阴性缺陷**，见 §4-1 |

## 3. 通过判据

- **P0（必须全绿）**：T01–T08、T11。
- **P1（功能正确性）**：T09 三组结果必须互不相同；T10 不得静默重复；T12 必须给出明确提示。
- **P2（已知缺陷，允许失败但必须记录）**：T13。

## 4. 缺陷清单与处理状态

### 4-0 结果卡片从未渲染（高，**已修复**，旧代码遗留）

**现象**：点完澄清选项后，气泡里只有一句"已为你找到 N 条相关资料（按相关度排序）："，
下面**没有任何结果卡片**。用户在 8010 上实测复现。

**排查**：先抓后端原始响应，确认数据完全正常——

```json
{"type": "result", "content": "已为你找到 5 条相关资料（按相关度排序）：",
 "documents": [{"id": 4107, "fileName": "东风_天龙_VECU_针脚定义【四插头】",
                "hierarchyPath": "整车->仪表模块->东风->天龙", "score": 9.8125}, ... 共 5 条]}
```

**根因**：`chat.js` 的 `appendResult(document, index)` **把形参命名为 `document`**，
遮蔽了浏览器全局 `document`，于是函数体里的 `document.createElement('div')`
变成"给普通对象调用不存在的方法"：

```
TypeError: document.createElement is not a function
    at ChatApp.appendResult (chat.js:776)
```

而 `sendMessage()` 的 `catch` 把它静默吞掉了（注释写着"接口已实现，不显示错误提示"），
所以控制台之外**看不到任何报错**。`appendResultFromHistory(document)` 有同样问题，
因此从历史恢复会话时卡片同样缺失。这是从 Java 版迁移过来就存在的旧缺陷。

**修复**：形参改名 `doc`；新增 `tests/test_frontend_contract.py` 静态守卫
（禁止任何方法形参叫 `document`，并禁止把后端字段直接拼进 `innerHTML`）。

**验证**：用 Node + 最小假 DOM 直接执行 `chat.js` 的渲染逻辑：

```
$ node verify_card_render.js
[OK] 卡片数量 = 1
[OK] 卡片子节点 = result-header, result-body
[OK] 标题 = 1 | 东风_天龙_VECU_针脚定义【四插头】
[OK] 明细行 = 资料 ID: 4107  /  层级路径: 整车 → 仪表模块 → 东风 → 天龙  /  匹配分: 9.81
```

### 4-1 硬 Metadata Filter 假阴性（高，**未修复**）
`build_metadata_filter()` 把 LLM 抽到的每个字段都作为**字面子串 AND 条件**。
实测 `"徐工仪表线路图"`：过滤后 0 条 → 直接回"没有找到资料"，而同一 Query 无过滤时
top5 含 `ID=8 徐工_XE135GII挖掘机_仪表显示器针脚定义`（知识库无"线路图"一词）。
**建议**：过滤结果为空时回退一次无过滤检索，并在 state 标记 `filter_fallback`。

### 4-2 `/api/select` 缺少中断保护（中，**已修复**）
无 pending interrupt 时直接 resume，LangGraph 从空 state 起跑，
`understand_query` 读 `state["original_query"]` → `KeyError` → 前端只看到泛化的
"选择处理失败，请重新发起查询"。
**修复**：`resume_search_workflow()` 先经 `_prepare_resume()` 判断
`snapshot.interrupts` / `snapshot.tasks[*].interrupts`；既无待处理项、也无法从历史
重建时，抛出明确的 `ValueError`（"当前会话没有可用的选择项…请重新输入查询"）。

### 4-3 澄清选项"点哪个都是同一个答案"（高，**已修复**）
**现象**：同一会话点 A 得 3 条，接着点 B、C 得到**完全相同的 3 条**。

**根因**（由 T09/T10 的对照实验定位）：
- T09（三个**独立会话**各点一个选项）→ 结果**互不相同**，说明 `build_facets` 分组与
  `apply_selection` 过滤逻辑本身是**正确的**；
- T10（**同一会话**连续点 1/2/3，`optionValue` 分别正确发送 `rank_group_0:1/2/3`）
  → 三次都返回第一次的结果。

即：第一次点击后 Graph 已跑到 `final_answer → END`，此时**不存在待处理的 interrupt**，
后续 `Command(resume=...)` 不会重新执行节点，而是直接返回**上一轮 checkpoint 的最终状态**。
前端又把该组按钮保持可点击，于是"每个选项都返回同一个答案"。

**修复过程与正确解法**（实测三种方案）：

| 方案 | 结果 |
|---|---|
| 直接对旧的 interrupt checkpoint `Command(resume=新值)` | ❌ 无效。LangGraph 会**重放**该 checkpoint 已写入的 resume 值，新值被静默忽略——连传非法选项都不报错 |
| 从 clarify **前一个** checkpoint 重新执行，再 resume | ✅ 有效，改选得到对应分组的结果 |
| 单次调用：从 clarify 前一个 checkpoint 直接带 resume 值起跑 | ❌ 返回空结果 |

**修复过程中还发现一个更隐蔽的问题**：一次**非法**选择会让 `clarify` 节点抛错，
而那个坏 resume 值会留在 checkpoint 里 —— 之后即便点合法选项也会重放坏值，
**整个会话被"毒化"**。

**最终实现**（`app/graph/service.py` 的 `_prepare_resume()`）：
无论首次选择还是改选，都按**选项值**找到最近一个
`values["option_groups"]` 里含该选项的 interrupt，从它的 `parent_config` 用
`ainvoke(None, ...)` 重新执行澄清节点（纯计算，不重新检索），生成一个**全新的
interrupt** 再 resume。统一"重建"而不是"直接 resume"，从根上绕开 resume 值重放。

| 场景 | 行为 |
|---|---|
| 首次点击 | 从澄清前 checkpoint 重建 interrupt 后 resume |
| 同一轮改选 | 同上，返回该选项自己的结果集 |
| **跨轮次改选**（第二轮出结果后点第一轮选项） | 回溯到**第一轮**那个含该选项的 interrupt |
| `__back__`（返回上一步） | 回溯到 `candidate_history` 非空的 interrupt |
| 非法选项值 | 不重建，交 Graph 抛"无效的选择项" |
| 非法之后再点合法选项 | 重建干净 interrupt，**可正常恢复** |

前端相应改为：实时选项与**从历史记录重建的选项**都可点击改选
（`chat.js` 的 `markSelectedOption()` / `appendOptionsFromHistory()`）。

### 4-4 澄清选项语义模糊（高，**已修复**）
**现象**：9 条候选时选项是"相关资料第 1/2/3 组（3条）"，用户无法据此选择。

**根因**：这些候选的 `hierarchy_path` 只有 `电路图->仪表模块->东风->天龙` 与
`整车->仪表模块->东风->天龙` 两种，路径维度只能切出 2 组，旧逻辑要求至少 3 组，
于是退化成按排名硬切；而真正的区分度在**标题**里（`仪表电路图` vs `VECU/BCM 针脚定义`）。

**修复**（`app/graph/nodes/facets.py`）：
1. 新增"**资料类型**"维度：按标题/路径中的类型词（整车电路图 / 电路图 / 针脚定义 /
   线束图 …）分组；
2. 维度选择改为"候选维度中取分组数最多者"，分组数相同优先资料类型；
   可用区间从 3～5 放宽到 **2～5**；
3. 路径维度下缺少该层级的资料归入"未分类"，保证选项覆盖全部候选（不再静默丢文档）；
4. 兜底分组不再叫"第 N 组"，改用组内首条真实标题。

效果：`"东风天龙仪表"` 的 9 条候选 → **针脚定义（5条） / 电路图（4条）**。

### 4-5 最终答案排版（中，**已修复**）
**现象**：答案挤成一行 `找到 3 条最相关资料： 1. [ID:4105] ... 2. ...`。

**根因**：正文含 `\n`，但 `.message-text` 没有 `white-space: pre-wrap`，换行被 HTML 折叠；
同时正文列表与下方结果卡片内容重复。

**修复**：
- 正文改为一句人话（`已为你找到 N 条相关资料（按相关度排序）：`），条目交给卡片；
- 卡片重构为"序号徽标 + 文件名主标题 + 资料 ID / 层级路径（`→` 分隔）/ 匹配分"分级布局；
- `.message-text` 增加 `white-space: pre-wrap`，保留后续任何多行文本；
- 卡片取值统一用 `textContent` 注入，顺带消除资料标题里的 HTML 注入面。

## 5. 运行方式

```powershell
cd D:\Study\chatbot-circuit-diagram
.venv\Scripts\python.exe scripts\smoke_test_api.py --base-url http://127.0.0.1:8010
# 追加 --json-out smoke_report.json 可保存原始响应
```

脚本退出码：`0` = P0 全绿；`1` = 存在 P0 失败。

## 6. 实测记录

### 6-1 修复前（`http://127.0.0.1:8010`）

| ID | 结果 | 观测 |
|---|---|---|
| T01–T08 | PASS | 静态、校验、分流、直答、澄清均正常 |
| T09 | PASS | 三个独立会话：`[4110,2333,2332]` / `[4105,4107,4109]` / `[4108,4106,4104]` |
| **T10** | **FAIL** | 连续点击 1/2/3 → 三次都是 `[4110,2333,2332]`（缺陷复现） |
| T11 | PASS | `无效的选择项，请重新选择当前选项` |
| T12 | WARN | `选择处理失败，请重新发起查询`（泛化错误） |
| T13 | WARN | `徐工仪表线路图` → "没有找到匹配的车辆电路图资料"（假阴性复现） |

T09 与 T10 的对照是定位 §4-3 根因的关键证据：**分组/过滤逻辑正确，问题出在图结束后
的 resume 语义**。

### 6-2 修复后（`http://127.0.0.1:8011`，加载修复代码的临时验证进程）

```
T01 PASS  T02 PASS  T03 PASS  T04 PASS  T05 PASS  T06 PASS  T07 PASS
T08 PASS  T09 PASS  T10 PASS  T11 PASS  T12 PASS  T13 WARN(已知缺陷)
P0 失败 0 / P1 失败 0 / 已知缺陷复现 1
```

T10 修复后行为：

```
点击 1 -> code=1 type=result docs=[4110,2333,2332]
点击 2 -> code=0 msg='当前会话没有待处理的选择（可能已完成，或服务重启导致会话状态丢失），请重新输入查询'
点击 3 -> code=0 同上
```

同时 `pytest` 36/36 通过、`ruff check --no-cache .` 全绿、`node --check frontend/js/chat.js` 通过。

### 6-3 待办

- 后端修复（`app/graph/service.py`）需要**重启 8010 进程**才生效；
- 前端修复（`frontend/js/chat.js`、`frontend/css/style.css`）是静态文件，
  **浏览器强刷（Ctrl+F5）**即可生效；
- 剩余未修复项：§4-1 的硬过滤假阴性（T13）。

### 6-4 第二轮：分组可解释 + 支持改选（§4-3 / §4-4 / §4-5 修复后）

用例扩充到 15 条（新增 T14 标签可解释、T15 多轮返回上一步），
在加载修复代码的临时进程上执行：

```
T01~T12 PASS   T14 PASS   T15 PASS   T13 WARN(已知缺陷)
P0 失败 0 / P1 失败 0 / 已知缺陷复现 1
pytest 46/46 ✅   ruff 全绿 ✅   node --check chat.js ✅
```

关键观测：

| 用例 | 修复前 | 修复后 |
|---|---|---|
| T08 澄清选项 | `相关资料第 1/2/3 组（3条）` | **`针脚定义（5条）` / `电路图（4条）`** |
| T09 独立会话 | 三组不同 | 三组不同（保持不变） |
| T10 同一会话改选 | 三次都返回第一次的结果 | 点 1 → `[4107,4109,4108,4106,4104]`；点 2 → **`[4110,2333,2332,4105]`** |
| T15 多轮 + 返回 | 第二轮 `其他（16条）` | 第二轮按品牌：`卡特彼勒(4)/日立(3)/斗山(2)/其他(12)`，返回后与第一轮完全一致 |

已知局限：`其他（12条）` 这类合并桶在候选品牌过多时仍会偏大；
选中它会进入下一轮更细的分组，属于可接受的收敛路径。

### 6-5 第三轮：改选的通用性（§4-3 收尾）

问题：改选是否只对某一个 Query 生效？结论——后端机制与 Query 无关（按选项值 +
checkpoint 历史驱动），但原实现有两个缺口，已修复：

| 缺口 | 修复 |
|---|---|
| 只回溯"最近一轮"的 interrupt，跨轮次回头点第一轮选项会报"无效的选择项" | 改为**按选项值**回溯到提供过该项的那一轮 |
| 前端把历史记录重建出来的选项渲染成 `disabled`，根本无法点击 | `appendOptionsFromHistory()` 改为可点击 |

最终验收（16 条用例，临时进程加载修复代码）：

```
T01~T12 PASS   T14 PASS   T15 PASS   T16 PASS   T13 WARN(已知缺陷)
P0 失败 0 / P1 失败 0 / 已知缺陷复现 1
pytest 50/50 ✅   ruff 全绿 ✅   node --check chat.js ✅
```

通用性实测（两个不同 Query、三个不同轮次）：

```
东风天龙仪表      : 首次选择 → [4107,4109,4108,4106,4104]
                    改选电路图 → [4110,2333,2332,4105]       (不同)
                    再改回     → [4107,4109,4108,4106,4104]  (一致)
                    非法值     → ValueError 无效的选择项
                    非法之后再点合法项 → 正常返回             (不再被毒化)

4HK1…针脚定义     : 第 1 轮 工程机械(21)/卡特彼勒(2)/三菱(1)
                    选工程机械 → 第 2 轮 卡特彼勒(4)/日立(3)/斗山(2)/其他(12)
                    选卡特彼勒(4) → [26,35,23,32]
                    回头改选第 1 轮卡特彼勒(2) → [403,402]    (跨轮次生效)
```

### 6-6 第四轮：结果卡片渲染（§4-0）

用户反馈"答案排版"问题的**真正根因**不在文案，而在卡片根本没渲染：

| 项 | 结论 |
|---|---|
| 后端 | 正常，`documents` 5 条齐全（已抓 8010 原始 JSON 验证） |
| 前端 | `appendResult(document, ...)` 形参遮蔽全局 `document` → `TypeError` |
| 为何一直没被发现 | `sendMessage()` 的 catch 静默吞异常，页面无任何提示 |
| 影响范围 | 实时结果与历史恢复**两条路径都受影响**，属旧代码遗留 |

修复后：`pytest 52/52`、`ruff` 全绿、`node --check` 通过、
Node + 最小假 DOM 直接执行渲染逻辑输出完整卡片结构。

> 这一条只需要**浏览器强刷（Ctrl+F5）**，不需要重启后端。
